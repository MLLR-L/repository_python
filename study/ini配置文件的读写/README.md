# 输出

``` python
输出元组，包括option的key和value [('配置1', '我是路径1'), ('配置2', '我是路径2')]
所有节点==> ['说明', 'CONFIG', '第三种方法', 'config']
CONFIG节点下所有option的key，包括默认option==> ['配置1', '配置2']
CONFIG下配置1的值==> 我是路径1
bitbucket.org下user的值==> 我是路径1
获取CONFIG下配置1的值==> 我是路径1
获取第三种方法值为数字的:ini2= 123456
包含实例范围默认值的词典==> OrderedDict()
循环节点说明下所有option==> [config]
循环节点说明下所有option==> 我叫option
判断CONFIG节点是否存在==> True
```

